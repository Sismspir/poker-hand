const HandleBets: React.FC<{ pot: number  }> = (pot) => {
    // Input: pot - each player's money - bets
    // calculates each player's money and adds the pot to the winner(s)
    // Returns: pot - each player's money left - bets
    // blinds are calculated inside the playerHand component
    return(
        <>
        </>
    )
}
export default HandleBets;